# AIエージェント向け共通指示 (AGENTS.md)

このファイルは、本リポジトリで作業するすべてのAIエージェント（Gemini CLI, Claude Code等）に対する共通の最優先指示書です。

---

## 1. コア原則

1. **正本（SSOT）と配信原稿の分離:**
   - 知識・事実・論証の正本は `books/` および `articles/` です。
   - SNS配信用の原稿は `social/posts/<id>.yaml` のみに記述し、正本を直接書き換える、または実行時にLLMで自動生成して即時投稿してはなりません。
2. **AIの権限境界:** 判断の基準は「**その操作より後に、人だけが通せる門が残っているか**」です。残っているなら AI が行ってよく、残っていないなら人の操作です。

   **人だけが通せる門は、`main` への PR のマージです**（2026-09-16 にこの形へ改めました。GitHub Environment の承認は外し、承認を PR のマージに一本化しました。手前に人の門を重ねても最終の門で必ず止まるため、防御は増えず作業だけが増えていたからです）。

   - **AI は `main` へ直接 push しません。** 作業はブランチで行い、PR を出します。マージを人が行うことが承認です。
   - **人だけが行うこと:**
     - **PR のマージ。** これが最終の門です。公開はすべてこの門より後ろで起きます。
     - `Reviewed-by` の記録。原稿を読んだのは人だという記録なので、AI が自分で付けてはいけません（H1、`lint/derive/review-policy.json` の `mode` が `human` のとき必須）。
     - **`main` の状態を変えずに公開へ至る操作。** つまり `social-publish.yml` の手動起動です。
   - **AI が行ってよいこと:** ブランチ上で公開スイッチを立てること（Zenn の `published: true`、Qiita の `private: false`、note と SNS の `status: ready`）。マージされるまで公開は起きません。`status: published` は投稿の結果の記録なので、AI は変えません。
   - 公開の「スイッチ」と、それが効く経路は媒体ごとに次のとおりです。いずれもマージが起点です。

     | 媒体 | 公開のスイッチ | マージ後に動くもの |
     | --- | --- | --- |
     | Zenn | `articles/*.md` の `published: true`、`books/*/config.yaml` の `published: true` | Zenn の GitHub 連携（`main` への反映で即公開） |
     | Qiita | `platforms/qiita/public/*.md` の `private: false`（未同期の記事は `private: true` か `ignorePublish: true` が必須。検査 Q10） | `publish-qiita.yml` |
     | note | `platforms/note/public/*.md` の `status: ready`（投稿後は人が `published` に変える） | `publish-note.yml` |
     | SNS | `social/posts/*.yaml` の `status: ready` と `revision` | `social-publish.yml`（マージ後に人が手動起動） |

3. **事実の捏造禁止（不変条件）:**
   - 正本（SSOT）にない数値、事実、実績、経験を、派生物（SNS投稿原稿、Qiita、note）に付け加えてはなりません。改訂前からある文でも、正本にも実装にも裏付けがない主張は削ります。

---

## 2. 媒体別チェックリストとルール

媒体ごとの規則は `lint/` 配下の設定（校正プロファイル・構造ポリシー・用語辞書）が正本であり、`npm run check` が機械的に検証します（設計は `docs/linting.md`）。以下は要点です。

### 2.0 Zenn（正本）・Qiita・note の基準
- **Zenn（正本）:** 題材はシステムに限らず広義の技術すべて。本ごとに genre（engineering / process / research / companion / essay）を `lint/policies/zenn.json` に登録し、genre が要求する要素（動作するコード・図・GitHub への導線・一次資料への出典など）と、作品が宣言した記述規範（例: 『未来予測の設計図』の序論・射程と本質・出典形式）を満たすこと。
- **Qiita（課題解決バリアント）:** `platforms/qiita/public/<id>.md`。技術選定理由の見出し、実装から逐語で取ったコード 1 箇所以上、公式ドキュメントへの外部リンク 1 ホスト以上、冒頭 40 行以内に正本（Zenn）または GitHub への導線。正本と同一の文が 30% を超えると重複コンテンツとしてエラー。
- **note（思想・ナラティブバリアント）:** `platforms/note/public/<id>.md`。正本のコピーではなく、意思決定の物語として別に書く。コードブロック・インラインコード・Mermaid・表・脚注は禁止。本文に正本（`canonical_url`）への導線を置く。frontmatter の `status` は AI は `draft` のみ。`ready` にできるのは人間だけ。
- **用語統一:** 題材別の辞書（`lint/terms/*.yaml`）に従う。表記を決めたら辞書に書き、自動検出の警告（長音・和欧間スペース）を消す。
- **アーキテクチャ図:** 図は **D2 で書いて PNG に描き出す**（`images/c4/<名前>.d2` → `npm run figures:render`）。ソース・PNG・再現用メタ情報（`.png.json`）の3点をコミットする。**mermaid でアーキテクチャ図を描かない**（Zenn は本文幅700pxを超える画像を縮小するため文字が潰れ、mermaid はコンポーネント図・コンテナ図の表現に向かない）。既定のレイアウトは TALA、一方向のフローは図ごとに dagre へ上書きする。PNG を手で差し替えてはならない（検査 D1〜D5）。手順と理由は `docs/diagrams.md`。
- **表現:** 複数称・組織称（私たち・弊社など）は全媒体で禁止。煽り・セールストーク（絶対・革命・100%・行動の強要）は SNS でエラー、Qiita / note で警告。
- **生成AIの利用の開示:** 生成AIで作成・改訂した原稿には、本文の冒頭に告知（Zenn は `:::message`、Qiita は `:::note info`、note は引用）を、最後の見出し「生成AIの利用について」に宣言（ツール名・用途と範囲・人による確認・責任の所在）を置く。本は最初の章に置く。ツール名と用途は、コミットの共著記録など確かめられる範囲で書き、確認していないことを「確認した」と書かない。**原稿を改訂したエージェントは、コミットの `Co-Authored-By` にツール名とモデル名を記録し（例: `Co-Authored-By: Gemini CLI (gemini-3.7-flash) <noreply@google.com>`）、宣言にも自分のツール名・モデル名と改訂した範囲（章・節の見出しか番号、コードのパス）を別の文で書き足す。** 既存のツールの文は書き換えない。開示の枠だけを足したモデルを、本文の作成に使ったと書かない（共著記録と宣言の突合を CI が双方向に検査する）。文例と根拠は `docs/ai-disclosure.md`（検査 Z8 / Q11 / N9）。

エージェントがSNSの投稿原稿（YAML）をドラフトする際は、以下の基準を機械的に検証し満たす必要があります。

### 2.1 LinkedIn 投稿基準
- **文字数:** レンダリング後の最終テキストが 1,600 文字までを推奨（2,400 文字で警告、3,000 文字超でエラー）。下限は置きません。
- **構造:** `フック ➔ 主張（結論） ➔ 根拠（具体例・比較） ➔ 実務への含意 ➔ 出口（URLや問い）` の5ブロックに沿っていること。冒頭140字に「記事を書きました」型の告知やURLを置かない。1〜3文で1段落とし空行で区切る。箇条書きは3〜5項目。本文のURLは1つまで。ハッシュタグは本文に書かない。
- **タグ:** ハッシュタグは `hashtags` フィールドに0〜3個記述。本文末尾にレンダラーが追加。同じタグの重複、無関係なトレンドタグは禁止。
- **表現:** 煽り、過度なセールストーク（「絶対」「革命」「100%」等）は禁止（`lint/policies/expressions.json`）。
- **導線:** `article.url` または本文に正本（`source.canonical_url`）を置く。UTM は原稿に書かない（配信時に付与）。
- **開示:** 本文に「※この投稿は、生成AI（ツール名）で下書きし、筆者が内容を確認して公開しています。」の 1 文を置く（SOCIAL_AI_DISCLOSURE）。

### 2.2 Bluesky 投稿基準
- **文字数:** 各投稿は 260 grapheme clusters までを推奨（1つの結合文字や絵文字を1文字として数える。最大300）。下限は置きません。
- **構成:** 1スレッドあたり1〜5投稿まで。1投稿目だけで主張が成立すること（「スレッドを開始します」「スレッドで解説します」のみの1投稿目は禁止）。1投稿1論点（4文まで）、1投稿1URL、ハッシュタグは1投稿0〜2個。
- **言語設定:** 日本語の投稿には必ず `langs: [ja]` を設定。
- **外部カード (`external`):** 一スレッドで最大1件まで。`external.url` または本文に正本への導線を置く。
- **画像 (`images`):** 一投稿で最大4枚まで。必ず `alt` を1〜1,000文字で付与し、画像サイズは 1MB 以下であること。
- **表現:** 煽り・セールストークは禁止。
- **開示:** スレッドのいずれかの投稿に「※生成AI（ツール名）で下書きし、筆者が確認して投稿しています。」の 1 文を置く（SOCIAL_AI_DISCLOSURE）。

### 2.3 派生物（Qiita・note・SNS）を作成・改訂する手順

派生物は、`npm run derive:qiita` と `npm run derive:note` で作ります。正本の文に ID を振り、各節で使ってよい文だけを Gemini に渡して白紙から書く方式です(帰属先行)。旧稿は渡しません。詳細と限界は `docs/derive-pipeline.md`。生成物は `.tmp/derive/` に出て、検証と判定を通ります。公開の場所へ移し、内容を読んで `Reviewed-by` を付けるのは人です(H1)。手作業で改訂する場合は、次の手順を上から順に行い、各手順を終えてから次に進みます。

1. **正本を全部読む。** 特に「既知の限界」の章と、但し書き（「〜していません」「〜だけです」）を読みます。
2. **主張を 1 文ずつ照合する。** 派生物の事実の文ごとに、正本の該当箇所か実装ファイルを特定します。特定できない文（他製品との比較、外部サービスの仕様や外部製品の性質、体験談、因果の説明）は、改訂前からある文でも削ります。
3. **規則に当たった文を言い換えでかわさない。** 同義語（手動→手作業、承認→確認、安全な基盤→安全な環境）、漢数字（600〜1,600 字→千文字前後）、婉曲表現（検知の回避→自動操作を補助、工数が増えた→作業の重さを実感）、但し書きの語を足すだけの修正は、同じ違反として扱います。正本の該当箇所の事実に書き直すか、文ごと削ります。削った文を受けていた接続（「同じ理由で」「しかし」など）も直します。直前の版と比べて、接続の語だけが残った文を V13 が止めます。照合リストの但し書きの語（「測っていません」「主張しません」）を定型文として足していくのも同じ違反で、主張そのものを削ります（N13）。
4. **正本の限界に反する書き方をしない。** 例: 実装済みでも配信経路に接続していない機能を「〜して送信します」と書かない。機械の照合リスト `lint/claims/<id>.json` が文単位で止めます（V8）。
5. **コードは実装から逐語で抜粋する。** 先頭行に、その言語のコメント記号でリポジトリ内のパスを書き（JavaScript は `// scripts/…`、YAML は `# .github/workflows/…`）、省略は同じ記号の `...` で示します。行末コメントも含めて書き換えず、訳や説明は本文に書きます。実装のコメントに正本で説明していない意図（検知の回避など）がある行は、載せずに `// ...` で省きます。実装の `@gate` の印の直後にある安全のための分岐は、それより後の処理を載せるなら省きません（Q12）。
6. **媒体の粒度を守る。** Qiita は選定理由・コアコード・導線のレシピ、note は判断の物語です。正本の統計、開発の経緯（コミットや共著の件数）、検査の全列挙を持ち込みません（V10）。規則に当たらず正本で裏付けられる文は、書き換えません。正本に選定理由がない技術に、理由を作って足しません。
7. **作業環境のパスを書かない。** `C:\Users\…`、`/home/…`、作業用ディレクトリ名は、ディレクトリ構成図やコマンド例にも書きません（Q13 / N10 / Z9）。
8. **生成AIの利用を開示する。** 冒頭の告知と末尾の宣言を置きます。宣言には、自分のツール名とモデル名、改訂した範囲（章・節の見出しか番号、変えた抜粋のコードのパス、題名を変えたなら「題名」）を、既存の文とは別の文で加えます。見出しは原稿のとおりに引き、「全体の改訂」「本稿の各節」のような総称は使いません。自分の文は「筆者が内容を確認し」「責任は筆者が負います」の文より前に置きます。既存のツールの文は書き換えず、共著記録にある他のモデル名（例: Claude Fable 5.1）も残します。開示の枠だけを足したモデルを、本文の作成に使ったと書きません。コミットには `Co-Authored-By: <ツール名> (<モデル名>) <メール>` を付けます（Z8 / Q11 / N9、`docs/ai-disclosure.md`）。git の設定（user.name、user.email、core.autocrlf）は変えません。コミットできなければ止まって報告します。
9. **依頼された原稿だけを変える。** 依頼にない原稿（`status: ready` 以降の SNS 原稿を含む）は、問題を見つけても変えずに報告します。
10. **コミットしてから `npm run check` をもう一度実行する。** 派生物の段階（qiita・note・variants）は警告も失敗になります。警告を「許容範囲」として残してはいけません。まとめに出る「検査した状態」の行（HEAD、index の tree、未コミットの変更の件数）を完了報告に貼ります。コミット前の結果は、コミットした状態の証明になりません。エラー 0・警告 0 は必要条件で、十分条件ではありません。手順 2 の照合を終えるまで完了としません。
11. **公開済みの記事に注意する。** Qiita で `private: false` の記事、note で `ready` 以降の原稿、`ready` 以降の SNS 原稿は、本文を最後に変えたコミット以降に人の `Reviewed-by` の記録がないと公開されません（H1）。作者は問いません。エージェントは `Reviewed-by` を自分で付けてはいけません。ただし `lint/derive/review-policy.json` の `mode` が `auto` のときは、機械の検査を確認とみなして H1 を通します(人の判断で切り替える設定)。
12. **LF の改行でコミットする。** 正本の題名と正本の「」の引用は、書き換えずに引用します（V11）。Qiita の構成図には main で追跡されているパスだけを書き（Q16）、題名やタグに掲げた技術（GitHub Actions など）は、その設定かコードを抜粋します（Q17）。note の強調は `**…**` だけを使います（N3）。見出しは 1 段ずつ下げます（Q15）。 CRLF でコミットすると差分が全行の書き換えに見え、人の査読を妨げます（Z10 / Q14 / N12）。

---

## 3. 作業フローと実行コマンド

エージェントはタスクを開始・完了する前後に必ず以下のコマンドを実行し、検証が 100% 合格であることを保証しなければなりません。ただし合格は必要条件であり、派生物では 2.3 の照合を終えるまで完了としません。

**実行には Node 20 以降が必要です（CI は 22）。** 古い Node では、検査の中身ではなく構文解析の段階で落ちるため「errors 0 なのに失敗」という読み解けない結果になります（`check-zenn.mjs` の絵文字判定が使う正規表現の `v` フラグと、`@atproto/api` の import 属性が、どちらも Node 20 以降の構文です）。`npm run check` は最初に版を確かめ、古ければその旨を出して止まります。

```bash
# 1. 全検査（媒体別校正・構成・図の可読性と再現性・日本語・用語統一・正本の構造・Qiita/note/SNS バリアント・媒体間の非対称）
#    12 段階すべてがエラー 0 であること。レポートは .tmp/lint/report.md
npm run check

# 2. SNS配信原稿（YAML）のスキーマ・セマンティクス・編集規則の検査（check にも含まれる）
npm run social:validate

# 3. ユニットテスト（SNS + linter）
npm test
```

警告（⚠）は検査を止めませんが、放置せず、規則が誤っているなら `lint/` の設定を、原稿が誤っているなら原稿を直してください。規則の閾値はコードではなく `lint/policies/*.json` と `lint/terms/*.yaml` にあります。

AIは、無関係なリファクタリング、全ファイルのコード整形、正本文面の一括改稿は行ってはなりません。
一度に1つのタスクIDのみを実行し、コミットメッセージにそのタスクID（例: `[SNS-003]`）を明記してください。
