# 基準日のベースライン記録 (2026-09-13)

媒体別 linter（`docs/linting.md`）を導入した時点の記録です。前回の基準は [baseline-2026-09-12.md](baseline-2026-09-12.md)。

- **Node.js バージョン:** `v22.23.2`
- **検査コマンド:** `npm run check`（11 段階）と `npm test`（SNS 30 件 + linter 45 件）

## `npm run check` の結果

| 段階 | 結果 | エラー | 警告 | 備考 |
| --- | --- | ---: | ---: | --- |
| `textlint` | ✅ | 0 | 1 | Bluesky 本文の「！」（SNS 規則では警告） |
| `books` | ✅ | 0 | 0 | 3 冊、118 章 |
| `figures` | ✅ | 0 | 0 | 109 図 |
| `japanese` | ✅ | 0 | 0 | 125 ファイル（Qiita / note の派生物を含む） |
| `links` | ✅ | 0 | 0 | 97 章 |
| `terms` | ✅ | 0 | 若干 | T3 和欧間スペースの併存（辞書に未登録の語） |
| `zenn` | ✅ | 0 | 若干 | QuickScribe の脚注に出典リンクがない章、SRB の出典なし章など |
| `qiita` | ✅ | 0 | 0 | 過去記事 8 件は対象外 |
| `note` | ✅ | 0 | 0 | 原稿 1 件（draft） |
| `variants` | ✅ | 0 | 2 | Qiita 記事に Zenn 正本へのリンクなし（正本が未公開のため保留）、長さが正本の 99% |
| `social` | ✅ | 0 | 若干 | 推奨文字数の下限、箇条書き 2 項目、ready の原稿が指す正本が未公開 |

正確な件数は `.tmp/lint/report.md` を参照してください。

## 導入時に原稿へ加えた修正

検査を通すために、次の原稿を直しました（内容の改稿ではなく、規則違反と描画事故の最小修正）。

- `README.md`: 用語の統一（`マークダウン`、`グラフェム`、`append-only`、`Approve（承認）` の語順、基盤の正式名）
- `books/pit-in-process/*.md`: 閉じない強調 `**文。 **次` を `**文。** 次` に修正（27 段落。アスタリスクがそのまま表示されていた）。用語の統一（`インタフェース`、`A(結果責任)`、`R(実行)`、`技術判断の記録(ADR)`、`特に`、`全て`、`本標準`、`共通原因`）
- `books/quickscribe-design/*.md`: 用語の統一（`ローカルLLM`、`フィルタ`、`モーメンタリ`、`——`、`kotoba-whisper v2.0`、`Ryzen 3700X`、`Common Voice/FLEURS`、`リッチにすると`、製品名と和文の間の半角スペース、数字と助数詞の密着）
- `books/sovereign-resilience-blueprint/*.md`: 段落直後の `---`（見出しに化ける）に空行を挿入（第Ⅴ部 15 章）。節番号の重複・欠番を修正（第Ⅱ部C-1、第Ⅴ部-10）。本文小節の `####` を `### 6.` に昇格（第Ⅰ部A-7）。章題の `）――` に半角スペース（第Ⅰ部A-4、A-7）。用語の統一（`手続き`、`の下で`、`概ね`、`に当たる`、`英国`、`国連`、`1箇所`、`第1に`、`第4の`、`集約型のインフラ`、`自律分散システム`、`汎用的な共通言語`）
- `articles/srb-appendix-glossary.md`、`articles/srb-appendix-engineer-role.md`: 本編の章の統合・並べ替えで古くなっていた章ラベルとリンク先（第Ⅲ部-12、第Ⅱ部C-12、第Ⅴ部-19 など存在しない章と、+1 ずれた第Ⅴ部のリンク 12 件）を修正。段落直後の `---` に空行を挿入
- `articles/multi-platform-publishing-architecture.md`: 誤植「配信 of の断片化」を修正、GitHub リポジトリへの導線を追加（engineering genre の要件）、`クロス・ポスティング`、フェンスの言語名
- `platforms/qiita/public/multi-platform-publishing-architecture.md`: 冗長表現・長文・太字の約物密着・「！」・「100%」・`純JavaScript` を修正、旧スクリプト名を更新。Playwright の選定理由の文は 120 字の規則に合わせて 2 文に分けたが、主張（マルチスレッド実行・storageState）は変えていない
- `social/posts/multi-platform-publishing-architecture.yaml`: 「私たち」→「私」、「100%安全な」の煽り表現を除去、`ピュアJS`（status は ready のまま。公開台帳に記録はなく未公開）
- `social/posts/pit-in-structure-2026-09.yaml`: 「爆速化」→「高速化」、`AI協調ループ` の和欧間スペース（draft）
- `social/fixtures/valid-thread.yaml`: 1 投稿目を「スレッドで解説します」型から主張が成立する文へ
- `docs/social-editorial-guide.md`: 良い例の本文にあった「私たち」を「私」へ

## 新規に追加した原稿

- `platforms/note/public/multi-platform-publishing-architecture.md`（status: draft）: note バリアントの初稿。正本のコピーではなく意思決定の物語として書き直したもの。

## 敵対的レビューで直した点（同日）

実装後にレビュー（5 観点、81 件の指摘）を行い、次を直しました。

- 位置引数のパス正規化（Windows のバックスラッシュ・`./`・絶対パスで 0 件検査になっていた）
- `--only` / `--report-dir` の値付き指定、段階クラッシュ時に古いレポートを読まない
- インデントされた・閉じ忘れたコードフェンスの認識、URL 直後の日本語・全角約物の切り出し、自動リンク `<https://…>` を HTML と誤認しない
- `--fix` が語幹だけに一致する規則で本文を壊す問題（`replacement` の導入）
- 辞書と煽り表現の誤検出（国連携・第Nの・氷の下で・米国債・産業革命・100% 一致・OpenAIエージェント・決済サービスなど）
- Qiita: 散文だけを数える Q1、画像とバッジを除く Q4、`text` を数えない Q5、コード内の例示を除く Q9、未同期記事の公開ゲート Q10、title の「！」
- note: GFM の簡略表の検出、一人称の部分一致、`social/posts` がなくても投稿できる、`publish_after` の gate
- 非対称: 本の章を正本とする Qiita の対応づけ、`--channel qiita`、公開状態の派生物と未公開の正本の警告（V6）
- SNS: URL 定義の共有、スレッド開始判定の誤検出、告知型の部分一致、上限値と複数称の一覧を `lint/policies` から読む
- CI: `publish-note` は status が ready に変わった原稿だけを対象にし（二重公開の防止）、post_id を環境変数で受けて検証、Windows ランナーで壊れる bash ステップを除去、`publish-qiita` の gate を Qiita だけに限定、`npm test` のキャンセル時実行を抑止、glob のクォート
