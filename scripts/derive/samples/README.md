# 生成サンプル

これは `scripts/derive/run-qiita.mjs` と `run-note.mjs` が正本から帰属先行で生成し、検査と判定を通した派生物のサンプルです。公開の場所（`platforms/*/public/`）の原稿ではありません。採用するには、人が内容を確認して `Reviewed-by` を付ける必要があります（H1）。生成の実体モデルは gemini-3.5-flash です（`--model gemini-3.7-flash` の指定が CLI で置き換わるため）。
